apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vault-glue-operator.fullname" . }}-test-connection"
  labels:
    {{- include "vault-glue-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vault-glue-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
